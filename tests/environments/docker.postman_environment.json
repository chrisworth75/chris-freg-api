{
  "id": "docker-env",
  "name": "Docker Environment",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:5100",
      "description": "Base URL for Docker deployed API",
      "enabled": true
    }
  ]
}